<%= content_tag :div, class: 'media', id: "item-#{item.id}" do %>
<% if item.name %>
  <div class="row">
    <p>
    <div class="col-md-6"><%= item.name %></div>
    
  </p>
  </div>   
<% end %>
<% end %>